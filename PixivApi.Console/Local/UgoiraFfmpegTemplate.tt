ffconcat version 1.0
<#
for (int i = 0; i < Frames.Length; ++i) {
#>
file '<# foreach (var c in Directory) { builder.Append(c == '\\' ? '/' : c); } #>/<#= i.ToString("D6") #>.jpg'
duration <#= Frames[i] #>ms

<#
}
#>